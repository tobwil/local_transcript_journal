# Server
PORT=4173

# Microsoft Teams (optional)
# Die Microsoft-Anmeldung und Client-ID werden direkt in der App eingerichtet.
TEAMS_SYNC_LOOKBACK_DAYS=90

# OpenAI (optional; without a key, a local heuristic is used)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
